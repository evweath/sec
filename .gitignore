# Plaintext scan directories — archived as encrypted blobs in memory/scans/
scan-*/

# macOS
.DS_Store

# Old memory location (migrated to memory/)
~/.claude/projects/

# Temp files
*.tmp
/tmp/

# Run artifacts
/logs/
__pycache__/
tools-zips/
scripts/childcustody.zip
sec/

# Oversized encrypted archives kept local-only (GitHub 100MB limit)
memory/scans/*-pcap-full.enc

# Rebuild run artifacts + license file (never leaves this Mac)
rebuild/logs/
rebuild/state/ls-registration.xpl
rebuild/LAST-RUN-MANUAL-STEPS.txt
